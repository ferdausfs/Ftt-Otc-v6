# R7.1 test artifact: baseline src tree is regenerated on demand by
# scripts/r71_tests.mjs from the baseline commit (git archive e56cd33 src —
# F3-20 refresh; marker file verify/baseline/.commit records the built SHA).
verify/baseline/

# FTT3 backtest: raw candle data (fetched, never committed) + in-sample audit
backtest/data/
results/audit_in_sample.jsonl
node_modules/

# FTT3-R EXT backtest: 12-month audit JSONL is committed only in .gz form
# (plain file is ~235MB — regenerate with: gunzip -k results/FTT3R_CRYPTO_EXT_audit.jsonl.gz)
results/FTT3R_CRYPTO_EXT_audit.jsonl
backtest/data_ext/
